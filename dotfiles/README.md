# dotfiles

The `install.sh` script will create symlinks to the config directories in `$XDG_CONFIG_HOME`. It also copies over the `check_dotfiles_update.sh` script
to `$HOME/.local/bin` to automatically check if the dotfiles are ever out of sync.
